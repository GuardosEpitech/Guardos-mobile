import { ReactDecorators, ReactFixtureExports } from 'react-cosmos-shared2/react';
import { CosmosConfig } from '../config/shared';
declare type UserModules = {
    fixtures: ReactFixtureExports;
    decorators: ReactDecorators;
};
export declare function getUserModules({ rootDir, fixturesDir, fixtureFileSuffix, }: CosmosConfig): UserModules;
export {};
