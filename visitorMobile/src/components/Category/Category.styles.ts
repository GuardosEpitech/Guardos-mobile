import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
  categoryBox: {
    marginVertical: 10,
    minWidth: "100%"
  },
  title: {
    fontSize: 20,
    fontWeight: 'bold',
    marginHorizontal: 10,
  },
});

export default styles;
